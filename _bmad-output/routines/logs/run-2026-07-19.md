# Run: Braking Physics + Gamepad Fixes — 2026-07-19

## Summary
Implemented realistic braking physics with speed-dependent scaling, weight-based stopping distances, and brake-induced understeer. Fixed Xbox controller gamepad support: corrected default LT/RT bindings from non-existent axes to buttons, added analog trigger reading, and added per-frame gamepad rediscovery to handle window blur connection drops. Updated README and standalone build.

## Agents

### 🔧 build (primary)
**Tools used:** Edit (14×), Read (8×), Grep (4×), Bash (10×), Task (3×), Question (1×), TodoWrite (5×)
**Files changed:** `src/ui/UIManager.ts`, `src/cars/CarConfigs.ts`, `src/physics/CarController.ts`, `src/input/InputManager.ts`, `src/input/InputManager.test.ts`, `specs/02-PHYSICS-SPEC.md`, `README.md`
**Commands run:** `npm test`, `npx tsc --noEmit`, `.\build-standalone.bat`, `git add`, `git commit`, `git status`, `git log`, `git diff`

#### Actions
- Edited `UIManager.ts` car card onclick to transition directly to CAR_PREVIEW (skipping intermediate select step)
- Reduced brakeForce values ~15x across all 4 cars in CarConfigs.ts (Rossini 2000→130, Weissach 2200→140, Kaiju 1900→110, Stingray 2400→125)
- Added braking constants to CarController.ts: BRAKE_SPEED_SCALE (10), BRAKE_MIN_EFFECTIVENESS (0.3), BRAKE_UNDERSTEER_FACTOR (0.4)
- Added `currentBrakeInput` class member to CarController
- Rewrote `applyBrake()` with speed-dependent scaling: `speedScale = max(0.3, min(1, speed/10))`
- Modified `applyGripForce()` to reduce lateral grip when braking: `brakeGripReduction = 1 - (currentBrakeInput * 0.4)`
- Updated physics spec (02-PHYSICS-SPEC.md) braking section, grip model, key constants, car parameters table
- Added `rescanGamepads()` private method to InputManager for per-frame gamepad rediscovery
- Modified `getState()` to call rescanGamepads() when gamepadIndex is null
- Modified `isAnyKeyPressed()` with same rescan fallback
- Changed default gamepad bindings from axis type (index 7/6) to button type (index 7/6) for Xbox LT/RT
- Changed `readGamepadAxis()` to read `button.value` (analog 0-1) instead of `button.pressed ? 1 : 0` (binary)
- Updated InputManager.test.ts to allow Escape shared between pause and back bindings
- Updated README.md: controls table (back=Escape, D-pad steer), physics model (braking, understeer), input section (gamepad rediscovery, menu nav), settings table (gamepad bindings), demo timeout (1→3 min), game flow note
- Ran `npm test` (88/88 pass), `npx tsc --noEmit` (no new errors), `.\build-standalone.bat` (success)

#### Narrative
The session started with a UX improvement (car select → preview shortcut), then pivoted to a major physics fix. The braking system was applying 1900-2400 N·s impulses per step at 120Hz, producing ~15g deceleration — cars stopped in under 0.2 seconds from 100 km/h. The fix reduced brake forces ~15x and added speed-dependent scaling so braking tapers at low speed, plus a grip circle model that reduces lateral grip when braking in corners (understeer).

Gamepad investigation revealed the input pipeline is 100% car-agnostic — the Weissach issue was a connection timing problem, not car-specific. Window blur during car selection nulled the gamepad index, and Chrome's Gamepad API requires a button press to reactivate. The fix adds per-frame rediscovery. The deeper trigger issue was that default bindings mapped throttle/brake to axes 6/7 (which don't exist — standard Gamepad API only has axes 0-3), when Xbox LT/RT are actually buttons 6/7. Fixed both the bindings and the analog value reading.

## Test Results
✅ 88 passed, 0 failed, 88 total (unit tests via Vitest)

## Commits
- `e739fed` — Fix gamepad menu navigation, steering, and listening state; add car select -> preview shortcut
- `c9c52fb` — Realistic braking physics, fix gamepad triggers, add gamepad rediscovery

## Notes
- Gamepad bindings changed from axis to button type — users with stored localStorage bindings will need to rebind in Settings
- BrakeForce values and BRAKE_UNDERSTEER_FACTOR are tuning knobs that may need further adjustment by feel in-game
- Pre-existing TypeScript errors in test files and AudioManager.ts (unused vars, missing names) remain unfixed
